<h1>LONGEST WORD-GAME</h1>

<div class="container">
  <p>Welcome to the longest word-game!</p>
  <p>Here is your grid:</p>
  <% @letters.each do |letter| %>
  <p class="letter"> <%= letter %></p>
  <% end %>
  <p>*****************************************************</p>
</div>

<div>
  <form class="container" action="/score" method="post" >
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <%= hidden_field_tag :letter_params , @letters%>
    <input class="question-bar" type="text" name="word">
    <input class="btn" type="submit" value="Play">
  </form>
</div>
